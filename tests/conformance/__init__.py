"""The host-contract conformance suite.

Asserts otto's shared *host contracts* against sampled ``(host, term,
transfer)`` cells, in one of the venues :mod:`tests.conformance._venue`
resolves.

Not to be confused with :mod:`otto.testing.conformance`, which is about
pluggable BACKEND INTERFACES conforming (``assert_lab_repository_conforms``,
``assert_reservation_backend_conforms``). Same word, different layer.
"""
