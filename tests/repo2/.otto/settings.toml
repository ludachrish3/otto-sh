name = "repo2"
version = "1.0.4"
lab_data_type = "json"
labs = [
    "${sut_dir}/../lab_data/tech2",
]
libs = [
    "${sut_dir}/pylib",
]
tests = [
    "${sut_dir}/tests"
]
init = [
    'repo2_instructions',
]

[docker]
registry_url = "docker.io"

[[docker.images]]
name = "worker"
dockerfile = "${sut_dir}/docker/Dockerfile"
context = "${sut_dir}/docker"

[[docker.composes]]
path = "${sut_dir}/docker/compose.yml"
default_host = "grape_seed"
services = ["worker"]
