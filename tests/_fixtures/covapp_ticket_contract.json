{
  "_comment": [
    "Shared Python<->TypeScript contract for the per-ticket coverage payloads.",
    "Four hand-maintained mirrors have no compiler tying them together: the",
    "data-format version (OTTO_COV_DATA_FORMAT vs EXPECTED_DATA_FORMAT), the",
    "keys spa_data.py emits vs the interfaces in web/src/covapp/types.ts, the",
    "sentinel ticket ids duplicated as TS string literals, and the names of the",
    "window callbacks the emitted chunk scripts invoke. This table is the single",
    "source of truth for all four, asserted from BOTH sides -- Python by",
    "tests/unit/cov/test_covapp_ticket_contract.py (against real emitted",
    "payloads, not a copied list) and TypeScript by",
    "web/src/covapp/contract.test.ts -- so a drift on either side fails exactly",
    "one suite and names the divergent language. Same shape as the",
    "format_outage_cases.json precedent."
  ],
  "data_format": 2,
  "sentinel_ticket_ids": ["(no ticket)", "(uncommitted)"],
  "chunk_callbacks": {
    "index": "__OTTO_COV__",
    "file": "__OTTO_COV_FILE__",
    "ticket": "__OTTO_COV_TICKET__"
  },
  "ticket_summary_keys": [
    "asserted",
    "chunk",
    "covered",
    "id",
    "owned",
    "per_tier",
    "uncovered",
    "url"
  ],
  "ticket_totals_keys": ["asserted", "covered", "owned", "per_tier", "uncovered"],
  "ticket_chunk_keys": ["files", "id", "stamp"],
  "ticket_chunk_file_keys": [
    "asserted",
    "asserted_only",
    "covered",
    "missing",
    "owned",
    "path",
    "per_tier"
  ]
}
