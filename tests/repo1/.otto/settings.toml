name = "repo1"
version = "1.0.0"
lab_data_type = "json"
labs = [
    "${sutDir}/../lab_data/tech1",
]
libs = [
    "${sutDir}/pylib",
]
tests = [
    "${sutDir}/tests"
]
init = [
    'repo1_instructions',
]

# Named OS profiles: bundle each Zephyr version's common attributes once so a
# host entry only declares its identity + filesystem instead of repeating
# osName / osVersion / command_frame / transfer / max_filename_len. The 2.7
# profile names the project-registered `zephyr-inline` dialect (registered by
# repo1_instructions), proving a data-defined profile can reference a
# code-registered command frame.
[os_profiles."zephyr-3.7"]
base = "embedded"
osName = "Zephyr"
osVersion = "3.7"
command_frame = "zephyr"
transfer = "console"
max_filename_len = 32

[os_profiles."zephyr-2.7"]
base = "embedded"
osName = "Zephyr"
osVersion = "2.7"
command_frame = "zephyr-inline"
transfer = "console"
max_filename_len = 32

[os_profiles."zephyr-4.4"]
base = "embedded"
osName = "Zephyr"
osVersion = "4.4"
command_frame = "zephyr"
transfer = "console"
max_filename_len = 32

[coverage]
gcda_remote_dir = "/var/coverage/product"

[docker]
registry_url = "docker.io"

[[docker.images]]
name = "api"
dockerfile = "${sutDir}/docker/Dockerfile"
context = "${sutDir}/docker"

[[docker.composes]]
path = "${sutDir}/docker/compose.yml"
default_host = "pepper_seed"
services = ["api"]
