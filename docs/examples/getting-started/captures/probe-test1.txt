$ otto --lab unix host test1 probe
INFO     fleet of interest: 0 of 4 lab hosts (1 repos, 1 excluded)                                  
capability       value      source
applet_base64    present    probed
applet_nc        present    probed
applet_poweroff  present    probed
applet_scp       present    probed
applet_shutdown  present    probed
applet_uudecode  absent     probed
applet_uuencode  absent     probed
base64_flag      -d         probed
checksum         md5sum     probed
elevation        sudo       probed
shell_dialect    bash       probed
stat_size        stat       probed
timeout_style    coreutils  probed

declared = already pinned in this host's userland_options, and never re-probed. probed = the device 
answered, so this one is worth pinning. assumed = otto could not ask, so the value is only what otto
did before it asked anything -- and it is left out of the pin for that reason, because inside a JSON
payload a guess is indistinguishable from a measurement.

Every capability settled. Pin these into this host's lab.json entry and the next connection to this 
device issues no probe at all:

"userland_options": {
  "applet_base64": "present",
  "applet_nc": "present",
  "applet_poweroff": "present",
  "applet_scp": "present",
  "applet_shutdown": "present",
  "applet_uudecode": "absent",
  "applet_uuencode": "absent",
  "base64_flag": "-d",
  "checksum": "md5sum",
  "elevation": "sudo",
  "shell_dialect": "bash",
  "stat_size": "stat",
  "timeout_style": "coreutils"
}
