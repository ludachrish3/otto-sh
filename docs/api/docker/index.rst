docker
======

The docker package provides image building, Compose orchestration, and
file staging for workflows that run containers on a remote parent host.

.. automodule:: otto.docker

.. toctree::

   build
   compose
   staging
