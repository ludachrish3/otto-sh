tls
===

The operating system's trust store for every HTTPS client otto or a backend
opens. For when and how to use it from a backend, see
:doc:`../cookbook/extending/https-clients`.

.. automodule:: otto.tls
   :members: DEFAULT_TIMEOUT_SECONDS, os_trust_context, os_trust_session
