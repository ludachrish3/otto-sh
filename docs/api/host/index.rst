host
====

The host package provides the core abstractions for running commands on
local and remote machines.

.. toctree::

   host
   remotehost
   localhost
   transfer
   session
   connections
   transport
   repeat
   telnet
