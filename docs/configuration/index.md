# Configuration

Two files describe everything otto knows about your project: a
`.otto/settings.toml` at the repository root, and the host data it names —
usually one or more `lab.json` files. `otto init` scaffolds both, and every
command reads them.

```{toctree}
settings
lab-config
host-options
os-profiles
declared-products-tools
host-sources
inventory
```
