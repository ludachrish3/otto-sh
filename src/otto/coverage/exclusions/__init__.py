"""Per-project coverage exclusion rules.

See ``docs/superpowers/specs/2026-08-23-coverage-exclusion-rules-design.md``.
Rules are declared in ``[coverage.exclusions]``, resolved against source by
``scan``, and applied to the merged store by ``apply``.
"""
